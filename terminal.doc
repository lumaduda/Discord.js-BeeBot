When you are on terminal say:

cd (drag folder) than click enter

After, say npm init

DO NOT keep on clicking enter and put your bot username and then follow the other things they gave you which is NAME, DESCRIPTION, VERSION, AUTHOR AND thats it.
